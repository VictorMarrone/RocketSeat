/* // Visualizar o conteúdo do document.

// console.log(document)

// Obter o title da página.

console.log(document.title)


// Acessar o elemento pelo ID (SELETOR ID)

const guest = document.getElementById("guest-2")
console.log(guest)

// Mostra as propriedades do objeto.

console.dir(guest)

// Acessar elemento com class (SELETOR class)

const guestsByClass = document.getElementsByClassName("guest")
console.log(guestsByClass)

// Exibir o primeiro elemento da lista

console.log(guestsByClass.item(0))
console.log(guestsByClass[0])


// Selecionar lista de elementos pela tag

const guestsbyTag = document.getElementsByTagName("li")
console.log(guestsbyTag) */

/* const guest = document.querySelector("#guest-1")
console.log(guest) */


// Retorna apenas o primeiro 

/* const guests = document.querySelector(".guest")
 */

// Retornas todos os elementos encontrados pela query

/* const guests = document.querySelectorAll(".guests") */
/* 
const guest = document.querySelector("#guest-1")
console.log(guest.textContent)
guest.textContent = "Ronaldo"
 */


// Adiciona a classe.

// const input = document.querySelector("#name")
// input.classList.add("input-error")

// Remove a classe.

// input.classList.remove("input-error")

// Se nao tem adiciona, se tem remove.

// input.classList.toggle("input-error")


// const button = document.querySelector("button")

// Modificar as propriedades CSS do elemento

// button.style.backgroundColor = "red"

/* const guests = document.querySelector("ul")


const newGuest = document.createElement("li")
newGuest.classList.add("guest")
const guestName = document.createElement("span")

guestName.textContent = "Victor"

const newName = document.createElement("span")
newName.textContent = "Marrone"


// newGuest.append(guestName) // Adiciona apos o ultimo filho
// newGuest.prepend(newName) // Adiciona antes do primeiro filho
// newGuest.append(guestName, newName)

// Mais simples e aceita apenas um.

newGuest.appendChild(guestName)

guests.append(newGuest)
 */


// MANIPULANDO ATRIBUTOS


// const input = document.querySelector("input")
// input.setAttribute("disabled", true)

// input.setAttribute("type", "file")

// input.removeAttribute("id")

/* window.addEventListener("load", () => {
    console.log("A pagina foi carregada")
})

addEventListener("click", (event) => {
    event.preventDefault()

    // Retorna todas as informacoes do evento.
   // console.log(event)

   // Retorna o elemento clicado.
    // console.log(event.target)
    // Retorna o textcontent do elemento clicado
    console.log(event.target.textContent)

}) */


    

    



















    




















